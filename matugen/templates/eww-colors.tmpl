/* Generated by matugen - do NOT hand-edit (clobbered on every setwall run). */
/* Consumed by ~/.config/eww/eww.scss via absolute-path @import. SCSS vars only. */
/* ASCII ONLY: eww's Sass engine injects (then rejects) a @charset rule if any  */
/* byte is non-ASCII, which silently drops every style. No em-dashes here.      */
/* month = low-contrast on_surface (dimmed with alpha in eww.scss); accent =     */
/* primary (box / circle / time / rule).                                         */
$clock-month: {{colors.on_surface.default.hex}};
$clock-accent: {{colors.primary.default.hex}};
$clock-fg: {{colors.on_surface.default.hex}};
