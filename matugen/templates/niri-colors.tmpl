// Generated by matugen — do NOT hand-edit (clobbered on every wallpaper/scheme change).
// Deep-merged over niri/colors-fallback.kdl via `include` in config.kdl.
// niri color strings need the leading '#', so tokens use the .hex form (NOT
// hex_stripped). Do not write a literal mustache example here: matugen evaluates
// every mustache tag even inside comments and aborts the whole render on a bad one.
layout {
    background-color "{{colors.background.default.hex}}"
    focus-ring {
        active-color "{{colors.primary.default.hex}}"
        inactive-color "{{colors.surface_variant.default.hex}}"
        urgent-color "{{colors.error.default.hex}}"
    }
    border {
        active-color "{{colors.primary.default.hex}}"
        inactive-color "{{colors.outline_variant.default.hex}}"
        urgent-color "{{colors.error.default.hex}}"
    }
    insert-hint {
        color "{{colors.primary.default.hex}}80"
    }
    tab-indicator {
        active-color "{{colors.primary.default.hex}}"
        inactive-color "{{colors.surface_variant.default.hex}}"
        urgent-color "{{colors.error.default.hex}}"
    }
}
