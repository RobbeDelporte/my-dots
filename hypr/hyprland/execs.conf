# Shell
exec-once = uwsm app -- wayle shell

# Desktop wallpaper clock (eww). launch.sh waits for awww, opens one window per
# output, and places them per eww/positions.conf.
exec-once = uwsm app -- ~/.config/eww/launch.sh

# Idle / lock: hypridle removed (manual-lock-only).

# Clipboard history watchers
exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store

# ydotool daemon (alternate-paste bind)
exec-once = uwsm app -- ydotoold

# Keyring and auth
exec-once = gnome-keyring-daemon --start --components=secrets
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1

# Auto delete trash 30 days old
exec-once = trash-empty 30

# Cursors
exec-once = hyprctl setcursor $cursorTheme $cursorSize
exec-once = gsettings set org.gnome.desktop.interface cursor-theme '$cursorTheme'
exec-once = gsettings set org.gnome.desktop.interface cursor-size $cursorSize

# Location provider and night light
exec-once = /usr/lib/geoclue-2.0/demos/agent
exec-once = sleep 1 && gammastep

# Forward bluetooth media commands to MPRIS
exec-once = mpris-proxy
